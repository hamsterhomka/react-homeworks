import React from 'react';
import PropTypes from 'prop-types';

function Spinner(props) {
  return (
    <div className="spinner" data-uk-spinner/>
  );
}

Spinner.propTypes = {};

export default Spinner;